
import { extractDate } from 'biannetta/helpers/extract-date';
import { module, test } from 'qunit';

module('Unit | Helper | extract date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = extractDate([42]);
  assert.ok(result);
});

